let value = 0; 
function setup(){
    createCanvas(displayWidth, displayHeight);
  }
  function draw(){
      //Background color
    background(169, 209, 212);    
    //Tally mark colors 
    stroke (255,215,0);
    strokeWeight(2);
  }
  